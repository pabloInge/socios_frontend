# PRODUCCION: API real de prod (Basic Auth contra {API_URL}/me).
# TODO: reemplazar la URL de prod cuando este disponible.
# Cargar con: pnpm dev:prod  |  pnpm build:prod

NEXT_PUBLIC_ENV=prod
ENV=prod
NEXT_PUBLIC_API_URL=https://TODO-API-PROD.example.com/api/v1
